# SJ Home Agent Environment Configuration

# Obsidian vault path
OBSIDIAN_VAULT=/Users/sjbaek/Desktop/001

# Output subfolder inside vault
OUTPUT_FOLDER=AI대화정리

# Chrome profile path (for Playwright persistent context)
CHROME_PROFILE=~/Library/Application Support/Google/Chrome/Default

# Database path
DB_PATH=data/sj.db

# Web server
WEB_HOST=127.0.0.1
WEB_PORT=8787

# LLM (optional, off by default)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# LLM_PROVIDER=none

# Vector search (optional, falls back to BM25-only if dependencies missing)
# VECTOR_SEARCH_ENABLED=true
# VECTOR_MODEL_NAME=intfloat/multilingual-e5-small
